# Staging values are supplied by the deployment environment.
# Do not commit real passwords or API keys here.
POSTGRES_DB=placemux_staging
POSTGRES_USER=placemux_staging
POSTGRES_PASSWORD=replace_in_secret_store
API_PORT=8000
